你现在要做的项目是C:\gitcloud\auto-lorr-new\lorr-code ，请牢牢记住项目位置到记忆和heartbeat.md
你要在项目src下新建一个notes目录然后读取C:\gitcloud\auto-lorr-new\test-script 里面的 SKILL.md，写到自己的skills名称叫大实验skill，也要记忆中记住 以后做实验必须做笔记，写入绝对路径到这个notes文件夹里面，用绝对路径以免建得到处都是的。


你现在要做C:\gitcloud\auto-lorr-new\lorr-code这个项目 ， schedule方向，C:\gitcloud\auto-lorr-new\lorr-code\src MAPF-workflow.md有项目信息，里面有部分具体文件路径改到C:\gitcloud\auto-lorr-new\test-script这个里面了，你需要修复文件路径，并且查询schedule方向能够改的文件，把schedule方向修改规范更新到workflow. 你浏览官网 和submission.md可以找到具体的信息，读读相关信息把可以改的文件找到。写入workflow
冠军代码在这个里面C:\gitcloud\auto-lorr-new\Best-Implementations-2024，然后workflow 是在 C:\gitcloud\auto-lorr-new\lorr-code\src下的 MAPF-workflow.md ，牢牢记住这两个位置到workflow和记忆里面


脚本测试目录在 C:\gitcloud\auto-lorr-new\test-script 里面，你要把脚本全部都可以跑通去测试项目里面得内容，并且测试后要无bug，测试分为每张地图测试和全图测试，你不仅要修复workflow的目录内容，还要修复每个脚本的功能，所有的脚本只把md5版本放进workflow里面提供使用，测试要但是要测试所有的脚本确保编译和各项功能都没有问题，为了节约时间，你脚本测试的时候步数都调整为200步，测试结束后还原回去，确保每个脚本最后功能都正常，你不需要做长步数测试。

编译环境自己去互联网下载。本地有clash verge如果被墙了可以连上去。

定时任务：
根据workflow开始大实验任务，记得研究每个地图特征，根据每个地图特征分开策略优化，别的团队schedule能比我们多2倍分数以上，不要懈怠，你的认知才是极限，本地分数远远没有到极限，停止懈怠，记笔记，拼命尝试
千万不要摸鱼，测试循环，以来人工，等待人工，所有的策略单独迭代，用env名称判断策略，一定要记得每个策略用env地图名判断，不然其他地图崩溃了，你一切都是白忙活。只有加了判断后才不用测和考虑其他图。
用类似这样的：判断bool should_use_my_planner(SharedEnvironment* env) {
    if (env == nullptr || env->map_name.empty()) return true;
    
    std::string name = env->map_name;
    std::transform(name.begin(), name.end(), name.begin(), ::tolower);
    
    // Use my planner for fulfill/warehouse maps (our optimized planner)
    if (name.find("warehouse") != std::string::npos || 
        name.find("fulfill") != std::string::npos) {
        return true;  // Use our optimized planner
    }
    
    // Use default starkit planner for room, maze, iron, bos, orz, random maps
    return false;
} 你应该一个地图优化1000轮再切下一个，重写算法优化到极致这样的，并且不判断env地图名就做等于白做，不记笔记实验等于白实验，所有策略算法都是根据地图名判断之后再写，不要忘记AWA原则,先研究后做实验，研究和写代码都很重要，沉淀笔记。